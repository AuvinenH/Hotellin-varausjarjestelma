# Optional absolute API base url (if frontend and backend are on separate origins)
# VITE_API_BASE_URL=http://localhost:5178

# Dev-server proxy target used when calling /api from frontend
VITE_PROXY_TARGET=http://localhost:5178
